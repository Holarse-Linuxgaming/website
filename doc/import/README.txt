Import

Verzeichnis in /tmp/export/ anlegen. Die Dateien aus dem Export-Tarball dorthin kopieren.

Tomcat mit Holarse starten. 

Dann die import-scripte laufen lassen. Der API-User aus 04_data.sql muss dabei in der Datenbank vorhanden sein.
